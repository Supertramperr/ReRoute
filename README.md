# ReRoute

Menu bar app for router reboot + connectivity status.

## Build
Open `ReRoute.xcodeproj` in Xcode and build/run.

## Repo notes
- This repo includes an `.xcodeproj` checked in.
- Local backups live under `Sources/_backups/` and are intentionally untracked.
